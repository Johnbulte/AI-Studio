# AI Studio OS Figma 26:773 像素级还原实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:executing-plans 在当前会话中逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** 将默认“创作工作台”首页校准到 Figma 节点 `26:773`，复用设计稿资源并保持现有其他页面和跳转不变。

**架构：** 继续使用现有 `Dashboard` 与 `Task` React 组件，不引入新路由或状态层。视觉校准集中写入 `src/styles/app.css` 末尾的 `Figma 26:773 fidelity pass` 覆盖块，资源继续从 `public/figma/dashboard/` 加载；测试锁定节点资源与关键画布坐标，浏览器截图负责最终像素验收。

**技术栈：** React 19、TypeScript 5、Vite 7、Vitest、Testing Library、纯 CSS、Tauri 2、Figma MCP。

**仓库说明：** 当前目录没有 `.git`，无法创建 worktree 或 commit。每个任务以测试、截图或构建结果作为检查点，不执行 Git 写操作。

---

## 文件结构

- 修改：`src/App.test.tsx` — 锁定首页资源来源、三栏边界和关键 Figma 坐标。
- 修改：`src/styles/app.css` — 仅在现有 `Figma 26:773 fidelity pass` 区域修正首页布局和视觉值。
- 必要时修改：`src/App.tsx` — 只修正首页 DOM 层级、文案或 Figma 资源引用，不改变聊天和图片页面。
- 复用：`public/figma/dashboard/*` — Figma 节点导出的图标、头像、缩略图、封面和积分环。
- 更新：`design-qa.md` — 记录 Figma 与本地实现同视口比较证据和最终结论。
- 生成：`artifacts/figma-26-773-implementation.png` — 本地首页 `1440 × 1024` 验收截图。

### 任务 1：建立首页还原回归测试

**文件：**
- 修改：`src/App.test.tsx`
- 测试：`src/App.test.tsx`

- [ ] **步骤 1：补充会失败的关键布局测试**

在现有 `locks the dashboard canvas and panels to the Figma coordinates` 用例中加入三栏和重点坐标断言：

```ts
expect(appStyles).toContain('.figma-sidebar { position: absolute; top: 0; left: 0;')
expect(appStyles).toContain('.figma-main { position: absolute; top: 0; left: 240px; width: 860px;')
expect(appStyles).toContain('.figma-tasks { position: absolute; top: 0; left: 1100px;')
expect(appStyles).toContain('.figma-hero { position: absolute; top: 40px; left: 40px;')
expect(appStyles).toContain('.quick-actions { position: absolute; top: 337px; left: 40px;')
expect(appStyles).toContain('.figma-tasks > .credits-card { position: absolute; top: 587.857px; left: 24px;')
```

- [ ] **步骤 2：补充 Figma 资源断言**

扩展 `uses node-specific Figma assets` 用例：

```ts
expect(screen.getByRole('button', { name: '充值积分' }).querySelector('img'))
  .toHaveAttribute('src', '/figma/dashboard/plus-small.svg')
expect(screen.getByRole('button', { name: '会员中心' }).querySelector('img'))
  .toHaveAttribute('src', '/figma/dashboard/membership.svg')
expect(screen.getByAltText('创意无限头像'))
  .toHaveAttribute('src', '/figma/dashboard/avatar.jpg')
expect(screen.getByTestId('workspace')).toHaveAttribute('data-node-id', '26:773')
```

- [ ] **步骤 3：运行测试并确认基线**

运行：`npm test -- --run src/App.test.tsx`

预期：若断言暴露缺失的坐标或资源则 FAIL；修正前保留失败输出作为基线。若现有代码已经满足断言，则记录 PASS，并以浏览器视觉比较作为失败门禁。

### 任务 2：校准首页 DOM 与 Figma 资源

**文件：**
- 必要时修改：`src/App.tsx`
- 复用：`public/figma/dashboard/*`
- 测试：`src/App.test.tsx`

- [ ] **步骤 1：逐项核对首页资源清单**

确认 `Dashboard` 与 `Task` 中的引用仅使用以下目录：

```text
/figma/dashboard/nav-*.svg
/figma/dashboard/task-*.svg
/figma/dashboard/avatar.jpg
/figma/dashboard/thumb-*.jpg
/figma/dashboard/project.jpg
/figma/dashboard/credit-ring.svg
/figma/dashboard/brand-art.png
```

不新增图标包，不以 CSS、字符或占位图替代这些资源。

- [ ] **步骤 2：仅在测试或 Figma 上下文不一致时修正 JSX**

首页结构必须保持以下层级，便于固定坐标和无回归跳转：

```tsx
<main className="figma-app" data-testid="workspace" data-node-id="26:773">
  <aside className="figma-sidebar">...</aside>
  <section className="figma-main">...</section>
  <aside className="figma-tasks">...</aside>
</main>
```

`AI 对话`按钮继续调用 `onOpenChat`，其他现有页面状态与组件不修改。

- [ ] **步骤 3：运行组件测试**

运行：`npm test -- --run src/App.test.tsx`

预期：全部首页文案、资源和跳转测试 PASS。

### 任务 3：按 Figma 坐标完成视觉校准

**文件：**
- 修改：`src/styles/app.css`
- 测试：`src/App.test.tsx`

- [ ] **步骤 1：锁定外层画布和三栏**

`Figma 26:773 fidelity pass` 覆盖块必须以这些值为最终计算结果：

```css
.figma-app { position: relative; display: block; width: 1440px; height: 1024px; overflow: hidden; }
.figma-sidebar { position: absolute; top: 0; left: 0; width: 240px; height: 1024px; }
.figma-main { position: absolute; top: 0; left: 240px; width: 860px; height: 1024px; padding: 0; }
.figma-tasks { position: absolute; top: 0; left: 1100px; width: 340px; height: 1024px; padding: 0; }
```

- [ ] **步骤 2：锁定中央主要模块坐标**

```css
.figma-hero { top: 40px; left: 40px; }
.prompt-card { top: 173px; left: 40px; }
.quick-actions { top: 337px; left: 40px; }
.dashboard-grid { top: 425px; left: 40px; }
.recent-panel { left: 0; width: 444.661px; }
.project-panel { left: 468.661px; width: 310.759px; }
```

- [ ] **步骤 3：锁定右栏与卡片坐标**

```css
.figma-tasks > h2 { top: 24px; left: 24px; }
.figma-tasks > .task-list { top: 68px; left: 24px; width: 292px; }
.figma-tasks > .all-tasks { top: 285px; left: 24px; width: 292px; }
.figma-tasks > .credits-card { top: 587.857px; left: 24px; }
```

- [ ] **步骤 4：运行静态回归测试**

运行：`npm test -- --run src/App.test.tsx`

预期：PASS。

### 任务 4：浏览器设计 QA 与修正循环

**文件：**
- 更新：`design-qa.md`
- 生成：`artifacts/figma-26-773-implementation.png`
- 必要时修改：`src/styles/app.css`

- [ ] **步骤 1：启动本地服务**

运行：`npm run dev -- --host 0.0.0.0 --port 4173 --strictPort`

预期：Vite 在端口 4173 启动，无编译错误。

- [ ] **步骤 2：使用内置浏览器打开首页并设置视口**

打开 `http://127.0.0.1:4173/`，视口设置为 `1440 × 1024`，等待字体与全部图片加载完成。

- [ ] **步骤 3：验证核心交互与控制台**

依次检查默认首页、提示输入框、快捷入口和 `AI 对话`跳转；浏览器控制台不得出现资源 404、React 错误或未处理异常。跳转后返回默认首页进行截图。

- [ ] **步骤 4：保存实现截图并进行同视口比较**

把实现截图保存为 `artifacts/figma-26-773-implementation.png`，与 `figma-reference-26-773-latest.png` 对比以下区域：

```text
0–240：品牌、导航、账户卡
240–1100：标题、创作框、快捷入口、最近创作、当前项目
1100–1440：任务列表、全部任务、积分卡
```

- [ ] **步骤 5：修复并复测所有 P0、P1、P2 偏差**

每次只修改 `src/styles/app.css` 的 Figma 覆盖块中与偏差对应的选择器，再重新运行 `npm test -- --run src/App.test.tsx` 并重新截图。结构错位、内容缺失、资源错误属于 P0/P1；可见尺寸、间距、颜色、裁切和字体差异属于 P2。

- [ ] **步骤 6：写入设计 QA 结论**

`design-qa.md` 必须包含源截图、实现截图、视口、比较区域、修复记录和以下终态之一：

```text
final result: passed
```

只有浏览器或截图能力确实不可用时才能写：

```text
final result: blocked
```

### 任务 5：完整验证与交付

**文件：**
- 验证：`src/App.tsx`
- 验证：`src/styles/app.css`
- 验证：`src/App.test.tsx`
- 验证：`design-qa.md`

- [ ] **步骤 1：运行完整测试**

运行：`npm test`

预期：所有 Vitest 测试 PASS。

- [ ] **步骤 2：运行类型检查**

运行：`npm run typecheck`

预期：退出码 0，无 TypeScript 错误。

- [ ] **步骤 3：运行生产构建**

运行：`npm run build`

预期：退出码 0，Vite 输出生产构建文件到 `dist/`。

- [ ] **步骤 4：确认最终 QA 门禁**

运行：`rg -n "final result: passed" design-qa.md`

预期：匹配到且只有一个最终通过结论。若结果为 `blocked`，不得宣称 1:1 还原已验证完成。

- [ ] **步骤 5：保持本地预览运行并交付**

提供本地预览地址、修改文件和验证结果，不发布、不部署，也不修改其他页面。
